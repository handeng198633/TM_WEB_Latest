<div class="nav">
    <div class="m-div clear">
       
        <a class="fl log">
        <div class="fl menu" style="margin-left: 120px;">
            <a href="/" class="fl menu-btn menu-btn-fire">忆游假日在线CMS</a>
            
            <div class="menu-drop">
                <a class="close" href="./忆游假期.html"></a>
                <a class="btn" href="">公司简介</a>
             
                <div class="tips">关于忆游假期</div>
            </div>
        </div>
        <% if logged_in? && current_user.id == 1 %>
                <% flash.each do |key, value| %>
                        <div class="alert alert-<%= key %>"><%= value %></div>
                <% end %>
                <div id="user-static-info-div" class="fr menu2">    
                    <a class="fl menu-btn menu-btn-fire border-radius-2" href="/signup">注册</a>
                    <li id="logo" class="dropdown fl menu-btn">
          
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">用户设置<b class="caret"></b>
                                </a>
                    <ul class="dropdown-menu">
                    <li><%= link_to "更新信息/密码", edit_user_path(current_user) %></li>
                    <li class="divider"></li>
                    <li>
                                        <%= link_to "退出", logout_path, method: "delete" %>
                    </li>
                                </ul>
                    </li>
                
        <% elsif logged_in? %>
                <% flash.each do |key, value| %>
                        <div class="alert alert-<%= key %>"><%= value %></div>
                <% end %>
                                    <li id="logo" class="dropdown fl menu-btn">
          
                                <a href="#" class="dropdown-toggle" data-toggle="dropdown">用户设置<b class="caret"></b>
                                </a>
                                <ul class="dropdown-menu">
                                    <li><%= link_to "更新信息/密码", edit_user_path(current_user) %></li>
                                    <li class="divider"></li>
                                    <li>
                                        <%= link_to "退出", logout_path, method: "delete" %>
                                    </li>
                                </ul>
                                    </li>
        <% end %>

    </div>
       
</div>
